export interface ShellPageSettings {
	/* A short feature classification e.g. identity */
	feature: string;
	/* Name of the page heading */
	name: string;
	/* Description of what the page does/represents */
	description: string;
	/* Icon of the page */
	icon: string;
}
